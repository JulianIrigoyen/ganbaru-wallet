description: Create a new Wallet
requestBody:
  content:
    application/json:
      schema:
        $ref: "../components/schemas/ConfirmWallet.yaml"
responses:
  '201':
    description: Wallet Created
    content:
      application/json:
        schema:
          type: object
          properties:
            wallet_id:
              type: string
              description: The ID of the created Wallet.